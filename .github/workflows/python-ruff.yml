name: autofix.ci
on:
  workflow_call:
  pull_request:
  push:
    branches: [ "main" ]
permissions:
  contents: read

jobs:
  autofix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      # Fix lint errors with https://github.com/charliermarsh/ruff
      - uses: install-pinned/ruff@76d990f7d5d8e6db806a8a6db30492cd519c1e06
      - run: ruff --fix-only .

      # Format code with https://github.com/psf/black
      - uses: install-pinned/black@66f034d8824bf0fe61b5f2541a53a5f7be39d914
      - run: black .

      - uses: autofix-ci/action@8caa572fd27b0019a65e4c695447089c8d3138b9

name: autofix.ci
on:
  workflow_call:
  pull_request:
  push:
    branches: [ "main" ]
permissions:
  contents: read

jobs:
  autofix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # Fix lint errors with https://github.com/charliermarsh/ruff
      - uses: install-pinned/ruff@a3218e7477f5e4afd14907abcac0ad344f89fe53
      - run: ruff --fix-only .
      - run: ruff format .

      - uses: autofix-ci/action@d3e591514b99d0fca6779455ff8338516663f7cc

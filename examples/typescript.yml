name: autofix.ci

on: [ pull_request ]
permissions:
  contents: read

jobs:
  autofix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - run: npm ci
      - run: npx prettier --write .

      - uses: autofix-ci/action@hash
